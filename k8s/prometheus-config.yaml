apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: streaming-platform
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: 'kubernetes-services'
        kubernetes_sd_configs:
          - role: endpoints

